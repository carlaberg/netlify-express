{
  "name": "netlify-express-root",
  "version": "1.0.0",
  "description": "",
  "dependencies": {},
  "scripts": {
    "prebuild": "cd backend/ && npm i && netlify-lambda build",
    "build": "cd frontend && npm install && npm run build"
  }
}
