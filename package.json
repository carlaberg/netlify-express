{
  "name": "netlify-express-root",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "netlify-lambda": "^1.4.10"
  },
  "scripts": {
    "dev": "cd frontend && parcel index.html",
    "prebuild": "netlify-lambda install backend && netlify-lambda build backend",
    "build": "cd frontend && npm install && npm run build"
  }
}
